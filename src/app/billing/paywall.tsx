import { ScreenPlaceholder } from '@/components/ui/screen-placeholder';

export default function PaywallScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Billing"
      title="Premium access"
      description="RevenueCat offering, plan comparison, and subscribe action will live here."
    />
  );
}
