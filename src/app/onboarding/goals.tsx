import { ScreenPlaceholder } from '@/components/ui/screen-placeholder';

export default function OnboardingGoalsScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Onboarding"
      title="Choose your goal"
      description="Personalize country documents for visit, work, study, family, business, or relocation."
    />
  );
}
