import { ScreenPlaceholder } from '@/components/ui/screen-placeholder';

export default function ProfileSettingsScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Profile"
      title="Account settings"
      description="Clerk account settings, language, notification preferences, and privacy controls will live here."
    />
  );
}
