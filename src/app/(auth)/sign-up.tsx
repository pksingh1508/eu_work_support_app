import { ScreenPlaceholder } from '@/components/ui/screen-placeholder';

export default function SignUpScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Auth"
      title="Create account"
      description="Clerk email and password sign-up will live here, including optional referral code capture."
    />
  );
}
