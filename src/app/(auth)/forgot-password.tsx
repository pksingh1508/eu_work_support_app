import { ScreenPlaceholder } from '@/components/ui/screen-placeholder';

export default function ForgotPasswordScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Auth"
      title="Reset password"
      description="Clerk password reset flow will live here."
    />
  );
}
