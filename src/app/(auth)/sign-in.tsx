import { ScreenPlaceholder } from '@/components/ui/screen-placeholder';

export default function SignInScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Auth"
      title="Sign in"
      description="Clerk email and password sign-in will live here."
    />
  );
}
